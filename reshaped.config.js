/** @type {import('reshaped').ReshapedConfig} */
const config = {
  // Removed custom themes to use Reshaped defaults
  themeOptions: {
    colorContrastAlgorithm: "apca",
  },
};

module.exports = config; 